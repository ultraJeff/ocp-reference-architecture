apiVersion: v1
kind: Service
metadata:
  name: myapp-api
  labels:
    app.kubernetes.io/name: myapp
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: myapp
    app.kubernetes.io/component: api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
